name: CI Python

# CI para proyectos Python. Mismo patron que ci-node:
#   - checks: typecheck (mypy si hay config) + lint (ruff si hay config)
#   - test:   pytest si hay carpeta tests/ o archivos test_*.py / *_test.py
# Sin asumir nada project-specific: cada step skip si el proyecto no trae la
# config correspondiente. Scan de imagen lo hace build-image.
on:
  workflow_call:
    inputs:
      python_version:
        description: Versión de Python.
        required: false
        type: string
        default: "3.11"

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}
          cache: pip
      - name: Install runtime deps
        run: |
          [ -f requirements.txt ] && pip install -r requirements.txt || true
      - name: Install dev deps
        run: |
          [ -f requirements-dev.txt ] && pip install -r requirements-dev.txt || true
      - name: Typecheck (mypy)
        if: hashFiles('mypy.ini') != ''
        run: |
          pip install mypy
          mypy .
      - name: Lint (ruff)
        if: hashFiles('ruff.toml') != '' || hashFiles('.ruff.toml') != ''
        run: |
          pip install ruff
          ruff check .
      # SAST CodeQL vive en codeql.yml (reusable aparte, lo llama el caller).
      # Scans SCA (Snyk OSS, Trivy fs) van aqui a futuro.

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}
          cache: pip
      - name: Install
        run: |
          [ -f requirements.txt ] && pip install -r requirements.txt || true
          [ -f requirements-dev.txt ] && pip install -r requirements-dev.txt || true
      - name: Test (pytest)
        shell: bash
        run: |
          # Existence check real: 'tests' es literal sin wildcard, nullglob no
          # lo elimina aunque el dir no exista. Usar -d / compgen.
          if [ -d tests ] || compgen -G "test_*.py" > /dev/null || compgen -G "*_test.py" > /dev/null; then
            pip install pytest
            pytest
          else
            echo "no tests detected, skipping"
          fi
